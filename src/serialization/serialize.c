#include "serialization/serialize.h"

void initSerializedGameData(char* gameData, size_t dataSize) {
    serializedData = (char*) calloc(dataSize, sizeof(char));
}

void addToSerialization(const char* name, void* data) {
    
}

char* serializeData() {
    
}